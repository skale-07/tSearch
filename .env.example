# GitHub API token (optional if `gh auth login` is done)
# GITHUB_TOKEN=ghp_your_token_here

# Assessment / LLM (Phase 1–2)
# LLM_PROVIDER=openai          # openai | anthropic
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
# LLM_API_KEY=sk-...           # fallback for the selected provider
# LLM_MODEL=gpt-4o-mini        # or e.g. claude-sonnet-4-5 when LLM_PROVIDER=anthropic
# ASSESSMENT_MOCK_LLM=1
# Preview teammates-name screen uses the same key/mock: one batched classify
# of unanchored page names (LinkedIn/GitHub URLs skip the model).
# ASSESSMENT_FORCE_REFRESH=1
# ASSESSMENT_CANDIDATE_LIMIT=10
# ASSESSMENT_REPOSITORY_LIMIT=3
# ASSESSMENT_PUBLICATION_LIMIT=3
# ASSESSMENT_ARTICLE_LIMIT=3
# Parallelism (raise carefully — GitHub + LLM rate limits apply)
# ASSESSMENT_CANDIDATE_CONCURRENCY=2
# ASSESSMENT_REPO_FETCH_CONCURRENCY=3
# GITHUB_DELAY_MS=800
# ASSESSMENT_RUNS_DIR=output/assessment-runs
# DIGESTS_DIR=output/digests
# DIGEST_TOP_N=10
# DIGEST_MIN_PRIORITY=50

# Digest email (Phase 7 / explicit send only)
# DIGEST_EMAIL_TO=you@example.com
# DIGEST_EMAIL_FROM=digest@yourdomain.com
# DIGEST_EMAIL_SUBJECT_PREFIX=[tSearch Digest]
# EMAIL_PROVIDER_API_KEY=re_...
# RESEND_API_KEY=re_...

# Reviewer feedback storage (digest ranking refinement)
# FEEDBACK_DIR=data/feedback

# Operator watchlist (look-at-later). Orthogonal to digest feedback.
# MARKS_DIR=data/marks

# System oracle (src/oracle): 1 enables live LLM synthesis of answers.
# Default is extractive mode — free, offline, cited passages only.
# ORACLE_LLM=1

# Coverage sweep + autopilot (continuous discovery)
# Pending seeds live in data/pending-seeds.json (olympiad CSV + rosters +
# manual cohort). Discover / `npm run resolve` runs the full discovery
# pipeline on a capped batch (LinkedIn + graph expand). Graph → Run pipeline
# re-runs known seeds. RESOLVE_ONLY=1 is opt-in for identity-only debugging.
# AWARD_SCRAPE_DELAY_MS=800     # pause between scholarship-roster HTTP fetches
# AWARD_SCRAPE_CACHE_TTL_MS=    # default 7 days; HTML cached under cache/award-rosters/
# PYTHON=python                 # interpreter for Discover → olympiad_winners.py
# OLYMPIAD_PULL_TIMEOUT_MS=900000  # 15m cap on UI olympiad re-pull
# RESOLVE_ONLY=1                # identity-only when calling runPipeline directly
# SWEEP_LIMIT=40                # pending names printed per `npm run sweep`
# AUTOPILOT_SEEDS_PER_RUN=5     # LinkedIn resolves per autopilot cycle (keep small)
# AUTOPILOT_SWEEP_LIMIT=25      # refresh pending sources per cycle (0 disables)
# AUTOPILOT_LIVE_LLM=1          # opt into live LLM assessment (default mock)
# AUTOPILOT_SEND=1              # opt into REAL digest email (default dry-run)
# DIGEST_PROFILE_BASE_URL=https://your-private-host/profiles  # hosted Learn-more pages (default: relative ./profiles/<digest_id>)

# Persistence (scaffolding only — JSON under data/ profiles/ output/ is still the source of truth)
# TSEARCH_STORE=fs                 # only `fs` is implemented; `supabase` throws until dual-write lands
# SUPABASE_URL=https://xxxx.supabase.co
# SUPABASE_ANON_KEY=eyJ...         # hosted Vite UI later; RLS deny-all until integration
# SUPABASE_SERVICE_ROLE_KEY=eyJ... # local pipeline writes only — never VITE_, never commit
