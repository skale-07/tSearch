# README screenshots

Save captures from the running app (`npm run dev` → http://localhost:5173) using these exact names:

| Filename | Shot |
|----------|------|
| `radial-tree.png` | Top bar + radial seed tree |
| `profile-panel.png` | Profile side panel open on a selected node |
| `assess-panel.png` | Assess panel open |

Referenced from the root [`README.md`](../../README.md).
