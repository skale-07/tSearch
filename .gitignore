
# Playwright (legacy LinkedIn MVP — not used by pipeline)
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/
cookies.json

# Pipeline output
output/
tmp/

# Frontend
web/dist/
web/node_modules/

# JSON storage (network-fetch cache + per-person records)
cache/
data/

__pycache__/
.history/
*.pyc
*.txt
.env
web/.env

# Local supabase CLI — never commit dumps or person data
supabase/.temp/
supabase/.branches/
# Scraped third-party PII — never commit (see docs/product-vision-and-direction.md §4)
profiles/
backup/
